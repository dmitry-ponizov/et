import styled from 'styled-components';

export const ExtraCardList = styled.div`
    padding: 16px;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
`;

export const ExtraInfoStyled = styled.div`
    height: 100%;
    display: flex;
    flex-direction: column;
`;