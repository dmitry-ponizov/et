import styled from "styled-components";

export const ProcessedDataStyled = styled.div`
    background: ${props => props.theme.colors.contentColor};
`;

export const ProcessedDataSection = styled.div`
    padding: 16px;
`;